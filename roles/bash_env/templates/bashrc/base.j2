################################################################################
# ansible controled section
# If you edit this file manually, the content in this section will disappear
# when playbook is executed.
# Therefore, do not edit this section manually,
# Do it in "Out of control from ansible" instead.
################################################################################


{{ lookup('template', 'bashrc/minimum.bash') }}


{% if customized_prompt | default(false) | bool %}{{ lookup('template', 'bashrc/customized_prompt.bash') }}{% endif %}

{% if customized_history | default(false) | bool %}{{ lookup('template', 'bashrc/customized_history.bash') }}{% endif %}

# enable color support of ls and also add handy aliases
# https://www.fulldigit.co.jp/server_env/ls_colors.html
if [ -x /usr/bin/dircolors ]; then
    test -r ~/.colorrc && eval "$(dircolors -b ~/.colorrc)" || eval "$(dircolors -b)"
    alias ls='ls --color=auto --group-directories-first'
    #alias dir='dir --color=auto'
    #alias vdir='vdir --color=auto'

    alias grep='grep --color=auto'
    alias fgrep='fgrep --color=auto'
    alias egrep='egrep --color=auto'
fi

# colored GCC warnings and errors
export GCC_COLORS='error=01;31:warning=01;35:note=01;36:caret=01;32:locus=01:quote=01'

# some more ls aliases
alias ll='ls -alF'
alias la='ls -A'
alias l='ls -CF'
alias lu='ls -ult'

# Add an "alert" alias for long running commands.  Use like so:
#   sleep 10; alert
alias alert='notify-send --urgency=low -i "$([ $? = 0 ] && echo terminal || echo error)" "$(history|tail -n1|sed -e '\''s/^\s*[0-9]\+\s*//;s/[;&|]\s*alert$//'\'')"'


################################################################################
# experimental
################################################################################


################################################################################
# Out of control from ansible
################################################################################

{{ out_of_control_content }}

