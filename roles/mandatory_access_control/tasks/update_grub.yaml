---
- name: Update grub
  # update-grub is usually used in debian family, not for Redhat family
  # So another task is required for Redhat family
  ansible.builtin.command: update-grub
  when: ansible_os_family == "Debian"
