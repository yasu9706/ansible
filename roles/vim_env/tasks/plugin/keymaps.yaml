---
- name: distribute plugin file 'keymaps.vim'
  ansible.builtin.copy:
    src: plugin/keymaps.vim
    dest: "~{{target_user}}/.vim/plugin/keymaps.vim"
    owner: "{{target_user}}"
    group: "{{target_user}}"

