- name: install package
  ansible.builtin.package:
    name: httpd
    state: present

- name: start service
  ansible.builtin.service:
    name: httpd
    state: started
    enabled: true

# RedHat系でSELinuxなどが有効な場合は、関連するタスクもここに追加
